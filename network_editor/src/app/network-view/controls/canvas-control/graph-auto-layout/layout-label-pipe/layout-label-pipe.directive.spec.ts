import { LayoutLabelPipeDirective } from './layout-label-pipe.directive';

describe('LayoutLabelPipeDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutLabelPipeDirective();
    expect(directive).toBeTruthy();
  });
});
